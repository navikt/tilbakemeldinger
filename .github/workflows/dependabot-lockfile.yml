name: Dependabot lockfile fix
description: Automatically update the lockfile when Dependabot opens a PR to keep it up-to-date and prevent merge conflicts. This is because of limitations to dependabot pnpm support with workspaces. Might be removed later if support is added to dependabpt.
on:
    pull_request:
        branches:
            - main

permissions:
    contents: write

jobs:
    update-lockfile:
        if: github.actor == 'dependabot[bot]'
        runs-on: ubuntu-latest
        steps:
            - name: Checkout PR branch
              uses: actions/checkout@v6
              with:
                  ref: ${{ github.head_ref }}
                  token: ${{ secrets.GITHUB_TOKEN }}

            - name: Setup pnpm
              uses: pnpm/action-setup@v4

            - name: Setup node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  registry-url: https://npm.pkg.github.com

            - name: Update lockfile
              run: pnpm install --no-frozen-lockfile
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.READER_TOKEN }}

            - name: Commit updated lockfile
              run: |
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"
                  git add pnpm-lock.yaml
                  git diff --cached --quiet || git commit -m "chore: update pnpm-lock.yaml"
                  git push
