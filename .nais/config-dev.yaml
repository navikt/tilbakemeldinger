---
apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: tilbakemeldinger
  namespace: {{namespace}}
  labels:
    team: personbruker
spec:
  image: {{image}}:{{version}}
  team: personbruker
  port: 8080
  ingresses:
  {{#each ingresses as |url|}}
     - {{url}}
  {{/each}}
  liveness:
    path: /person/kontakt-oss/tilbakemeldinger/internal/isAlive
    initialDelay: 10
  readiness:
    path: /person/kontakt-oss/tilbakemeldinger/internal/isReady
    initialDelay: 10
  replicas:
    min: 1
    max: 1
  webproxy: true
  azure:
    application:
      enabled: true
  tokenx:
    enabled: true
  idporten:
    enabled: true
    sidecar:
      enabled: true
      level: Level3
  accessPolicy:
    outbound:
      rules:
        - application: tilbakemeldingsmottak-api
          namespace: teamserviceklage
        - application: nav-dekoratoren
          namespace: personbruker
      external:
        - host: dekoratoren.ekstern.dev.nav.no
        - host: norg2.dev-fss-pub.nais.io
  leaderElection: true
  env:
    - name: ENV
      value: dev
    - name: API_URL
      value: http://tilbakemeldingsmottak-api.teamserviceklage
    - name: NORG2_URL
      value: https://norg2.dev-fss-pub.nais.io
  resources:
    limits:
      memory: 512Mi
    requests:
      cpu: 50m
      memory: 96Mi
