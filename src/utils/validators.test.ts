import { describe, expect, it } from '@jest/globals';

// Simple test to verify Jest works in the src directory
describe('Client Validators', () => {
    it('should be able to run tests in src directory', () => {
        expect(true).toBe(true);
    });
});
